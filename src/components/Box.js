import React from 'react';
import { VStack } from '@chakra-ui/react';

export const Box = () => (
    <VStack
        w="full"
        h="full"
        p={10}
        spacing={10}
        alignItems="flex-start"
        bg="gray.50"
    >
    </VStack>
);
